import org.junit.Assert;
import org.junit.Test;

/**
 * The class of FirstTest only contains one dummy test.
 * Only used to see if the 'mvn test' command is running or not.
 *  @author  Isabel Redtzer
 * */
public class FirstTest {

    /**
     * firstTest() tests that true assertsTrue - this test will always pass.
     * */
    @Test
    public void firstTest(){
        Assert.assertTrue(true);
    }
}
