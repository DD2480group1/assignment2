import org.junit.Assert;
import org.junit.Test;

public class FirstTest {

    // Dummy test which will always pass to test if the others are working.
    @Test
    public void firstTest(){
        Assert.assertTrue(true);
    }
}
